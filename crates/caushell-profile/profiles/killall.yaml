dsl_version: caushell.profile/v1alpha1
kind: command_profile

identity:
  canonical_name: killall
  aliases: []

trust:
  tier: tier_a
  source: built_in

platform:
  os_families:
    - posix
    - linux
    - macos
  shell_families: []
  requires_features: []

forms:
  - id: process_name_targets
    selector:
      kind: has_positional_at
      index: 0
    parameters:
      - name: targets
        semantic:
          kind: process_target
          target_kind: process_name
          broad_match: true
        binding:
          kind: remaining_positionals
        cardinality: required_many
    effects:
      - kind: control_process
        target:
          kind: slot
          name: targets

modifiers:
  - id: signal
    matcher:
      kind: any_flag
      flags:
        - "-s"
        - "--signal"
    parameters:
      - name: signal_spec
        semantic:
          kind: plain_value
        binding:
          kind: following_matched_flag
          operand_mode: next_arg
        cardinality: required_one

subcommands: null
extensions: {}
