[package]
name = "caushell-types"
version.workspace = true
license.workspace = true
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
